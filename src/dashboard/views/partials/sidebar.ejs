<aside class="fixed left-0 top-0 h-screen w-64 bg-gray-800 border-r border-gray-700 pt-20 overflow-y-auto">
    <nav class="p-4 space-y-2">
        <a href="/guild/<%= guildId %>" 
           class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= active === 'overview' ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700' %>">
            <i class="fas fa-home w-5"></i>
            <span>Overview</span>
        </a>
        
        <a href="/guild/<%= guildId %>/analytics" 
           class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= active === 'analytics' ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700' %>">
            <i class="fas fa-chart-line w-5"></i>
            <span>Analytics</span>
        </a>
        
        <a href="/guild/<%= guildId %>/economy" 
           class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= active === 'economy' ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700' %>">
            <i class="fas fa-coins w-5"></i>
            <span>Economy</span>
        </a>
        
        <a href="/guild/<%= guildId %>/moderation" 
           class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= active === 'moderation' ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700' %>">
            <i class="fas fa-shield-alt w-5"></i>
            <span>Moderation</span>
        </a>
        
        <a href="/guild/<%= guildId %>/settings" 
           class="flex items-center gap-3 px-4 py-3 rounded-lg transition <%= active === 'settings' ? 'bg-blue-600 text-white' : 'text-gray-300 hover:bg-gray-700' %>">
            <i class="fas fa-cog w-5"></i>
            <span>Settings</span>
        </a>
        
        <div class="pt-4 mt-4 border-t border-gray-700">
            <a href="/dashboard" 
               class="flex items-center gap-3 px-4 py-3 rounded-lg text-gray-300 hover:bg-gray-700 transition">
                <i class="fas fa-arrow-left w-5"></i>
                <span>Back to Servers</span>
            </a>
        </div>
    </nav>
</aside>
